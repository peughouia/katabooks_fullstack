services:
  db:
    image: postgres:15
    container_name: katabooks-db
    environment:
      POSTGRES_DB: katabooks
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: